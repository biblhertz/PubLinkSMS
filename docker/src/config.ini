; ============================================================
; AUTO-GENERATED — do not edit this file directly.
;
; This file is written by php/php-fpm-entrypoint.sh to /var/www/config.ini
; at container startup from environment variables defined in .env.
;
; To change these settings:
;   1. Run init.sh (Linux) or init.bat (Windows) to create/update .env
;   2. Rebuild and restart the containers
;
; Environment variables and their defaults:
;
;   [credentials]
;   API_USERNAME      — basic-auth username (legacy fallback)
;   API_PASSWORD      — basic-auth password (legacy fallback)
;   API_KEY           — API key for X-API-Key header auth
;
;   [path]
;   FILE_STORE_URL         — external URL of the manifest store
;   INTERNAL_FILE_STORE_URL — internal Docker URL (default: http://web:80/iiif_manifests)
;   IIIF_VALIDATOR         — validator service URL (default: http://validator:8080/validate?version=3.0)
;   PUT_MANIFEST           — internal put endpoint (default: http://web:80/api/v1/putManifest)
;   REMOVE_MANIFEST        — internal remove endpoint (default: http://web:80/api/v1/removeManifest)
;
;   [series]
;   VALID_SERIES      — comma-separated list of valid series names
;   FLEXIBLE_SERIES   — comma-separated list of flexible series names (optional)
;
;   [environment]
;   ENVIRONMENT       — 1 = local docker, 2 = remote docker
;
;   [database]
;   DB_HOST           — database host (default: mysql)
;   DB_NAME           — database name (default: manifest_server)
;   DB_USER           — database user (default: manifest_user)
;   DB_PASSWORD       — database password (required — set in .env)
; ============================================================
